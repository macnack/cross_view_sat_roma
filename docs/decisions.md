# Decisions log (append-only; date, decision, reason)

- 2026-09-17 — LiDAR is training-only. Inference uses spherical camera + IMU roll/pitch + camera height. Reason: deployment cost; LiDAR teaches geometry, is not needed to use it.
- 2026-09-17 — No dense depth network. BEV built by gravity-aligned IPM; buildings placed by a predicted wall–ground contact line; facade features collapsed onto the footprint; elevated content without ground contact masked. Reason: depth only decides placement; contact line + camera height suffice for orthophoto matching (H2). Raw IPM alone is NOT the method (BevSplat evidence).
- 2026-09-17 — Geometry head on the shared image encoder predicts per-patch normal (gravity frame), planarity, contact line, confidence; labels from LiDAR plane fits. Azimuth encoded as (cos 2α, sin 2α).
- 2026-09-17 — Matcher: Sat-RoMa multi-hypothesis refinement reused; solver 2-point SE(2) RANSAC (scale from camera height), homography as fallback.
- 2026-09-17 — Training losses: Sat-RoMa coarse loss + orientation-aware contrastive alignment on pre-decoder features (hard negatives: same edge orientation within 20 m) + geometry losses. Contrastive term is the main method contribution.
- 2026-09-17 — Lift-Splat (Dur360BEV's SI2BEV) is scaffolding and the learned-lifting ablation row, never the method.
- 2026-09-17 — Kick-off go/no-go: H1 test with LiDAR-oracle BEV and raw IPM BEV on the unchanged Sat-RoMa checkpoint. Small gap → geometry story; large gap → paper is about closing it; neither works → stop.
- 2026-09-17 — Dur360BEV ships no camera↔LiDAR↔INS extrinsics (kick-off page was wrong). Start from SI2BEV's axis permutation; measure residuals on data; lever arm and camera height estimated from data and logged with uncertainty.
- 2026-09-17 — Orthophoto source: Environment Agency VAP (OGL v3) if Durham is covered; Aerial Digimap via a Durham collaborator as fallback (not redistributable). No Google/Bing/Esri tiles.
- 2026-09-17 — Venue: ICCV 2027 if the geometry head transfers to VIGOR, otherwise IROS 2027 / RA-L; deadlines coincide (early March 2027).
- 2026-09-17 — BEV grid: 224×224 cells at 0.25 m (56 m extent, radius 28 m); reference 896 px at the same GSD (224 m). Reason: query must be 224 px for the matcher and 56 m sits inside the checkpoint's 35–70 m training band. Supersedes "radius 40 m".
- 2026-09-17 — Oracle BEV has two variants: (a) LiDAR-only, cells without returns invalid; (b) LiDAR + IPM ground fill where LiDAR has no return. Reason: wet asphalt gives almost no LiDAR returns (frame 0: 27 % return rate, none on the road). Both are reported.
- 2026-09-17 — Camera height: estimated only from frames with road returns (LiDAR ground-plane height) plus the fitted camera–LiDAR z-offset; logged with uncertainty.
- 2026-09-17 — Stored OxTS yaw convention is settled empirically by direction of travel and documented in data/README.md. Device convention per RT3000 manual: vehicle x fwd / y right / z down, heading clockwise from north; Dur360BEV's map_api suggests the files store radians CCW from east.
- 2026-09-17 — environment.yml is for Eagle only; local work uses the existing `bev-patch-pf` conda env.
- 2026-09-17 — Validity mask explicitly excludes the ego-vehicle blind disc and the dual-fisheye stitching seam; extents are logged.
- 2026-09-17 — Dataset subset: the HF release is one 170 GB gzip'd tar ordered by modality, so no sequence-level download exists. We stream it once (scripts/fetch_dur360bev_subset.sh) and keep every 10th frame, dense frames 0–299, all OxTS/timestamps/metadata.
- 2026-09-17 — ERP resolution 1280×640 (source fisheyes are 640 px; Dur360BEV's 2048×1024 default only upsamples).
- 2026-09-17 — Fisheye lens model: use Dur360BEV's piecewise model (196° inner / 203° outer FOV, knee 1.0 rad) from their unused `dualfisheye_project`, not fisheye_tools' ideal 203° equidistant remap. Reason: with identity extrinsics the 196/203 model puts LiDAR points on poles/cars/pedestrians to ~1 px; the 203° remap is ~6 px off at 57° off-axis. Our ERP is now built from this model (`Dur360Frames(lens="dur360")`).
- 2026-09-17 — Camera←LiDAR extrinsic: R = I, t = (0, 0, −0.27) m, i.e. camera 0.27 m above the LiDAR origin. Set visually by Maciej in the overlay viewer; Claude's A/B on near points confirms the sign and |t_z| in 0.13–0.27 m, t_y = −0.03 ± 0.09 m ≈ 0. Automatic edge-score and parallax-regression fits were rejected (both failed an inject-and-recover test). Cross-check by ambient-image↔ERP PnP is pending; value is provisional until then.
- 2026-09-17 — LiDAR height above road 1.57 ± 0.05 m (preliminary, 5 frames; road vs pavement unresolved) → camera height 1.84 m for IPM. Wet asphalt returns almost nothing (median 18 % return rate in road sectors over 275 frames), so this needs re-estimation on drier/better frames.
- 2026-09-17 — Sat-RoMa checkpoint for H1: released `4adxis71` (ACIVS paper: query 35–70 m, ±55°, scale 0.65–1.35), loaded via ~/Github/sat-roma-infer; `checkpoints/satroma_dinov3.pt` does not exist locally.
- 2026-09-17 — Ground handling: LiDAR ground segmentation (local lowest-height floor, 0.5 m cells, ±0.20 m) → per-azimuth contact line v_c(u) = lowest non-ground return per ERP column; IPM is applied only to pixels below v_c(u). Reason: raw IPM smears every above-ground object radially; "no return = ground" leaks on glossy objects. The LiDAR contact line is also the training label the geometry head must later predict, so this variant is the oracle upper bound of the camera-only method.
- 2026-09-17 — Primary kick-off query = "IPM below contact line" (`ipm_cl`: camera geometry + oracle contact line; Maciej: looks best). Reported alongside it: `ipm_raw` (lower bound), `oracle_a` (LiDAR only), `oracle_b` (ipm_cl + LiDAR objects). Known consequence: the query is mostly road surface (17–38 % of the grid valid), so along-road position on straight streets is expected to be weakly constrained.
- 2026-09-17 — ERP validity mask: ego vehicle from 175 moving frames (temporal std < 5 OR |temporal mean of high-pass| > 6, per-column envelope, 6 px margin) = 30 % of the ERP; seams ±1.3° at azimuth ±90°. Ground first visible ≈3.1 m ahead, 1.8 m to the sides, 7.1 m behind (at camera height 1.84 m). File: experiments/00_calib/erp_valid_1280x640.npy.
- 2026-09-17 — Contact line = FOOT of the lowest near-floor return per ERP column (its own x, y dropped to the local floor), not "row of the lowest object return ∩ flat ground". Reason: the bottom 0.2 m of every wall is labelled ground by the tolerance, which overshot contact range by 0.2/1.57 ≈ 13 % (1 m at 8 m); found by a unit test. Only returns < 0.7 m above the floor define a contact, so overhanging canopy/signs do not cut off visible ground.
- 2026-09-17 — Above-contact content is represented as thin WALL EDGES at the contact feet of objects taller than 1.5 m (`ipm_edge`), per Maciej: a wall is a line in a top-down view; its height is irrelevant for matching. The radial "collapsed facade" bands (`ipm_fp`) are rejected (fans; first obstacle only; arbitrary depth) and kept only for the record.
- 2026-09-17 — Multi-frame mosaic: BEVs warped by relative SE(2) pose, nearest-range-wins compositing, edges OR-ed. Poses from KISS-ICP (de-skewed with per-column timestamps) until OxTS is available. 56 m grid coverage at frame 200: 46 % single → 71 % with frames at +1/+5/+10 m.
- 2026-09-17 — Code refactored: parameters in configs/default.yaml, one Makefile target per step, run snapshots in every output folder; rejected calibration fits moved to scripts/attic with reasons.
- 2026-09-19 — Query representation: H1 keeps the RGB BEV. Projecting DINOv3 features into the BEV instead (Maciej's proposal; FG2's design) is deferred to the geometry-head task, where the lifting module is trained. Reasons: (a) the checkpoint's decoder was trained on overhead↔overhead `sat493m` features, and IPM exists precisely to synthesise the nadir view — lifting ERP features would feed it ground-view features against an overhead reference, the cross-view gap FG2 closes by training, so it cannot be evaluated with the frozen checkpoint and it retires H1 ("unchanged matcher"); (b) the lift must be `grid_sample` at sub-patch resolution, never the patch grid: at stride 16 on the 1280×640 ERP only six patch rows see ground (46.8 / 15.5 / 9.3 / 6.5 / 5.0 / 4.0 m, row 21 alone spanning 11.6–23.4 m), which fills 33–56 of the 196 query patch cells and 0 of the 76 cells beyond 20 m (frames 200/1000/2000), whereas one ERP pixel row spans 0.28 m of range at 10 m and 2.1 m at 28 m. The decisive no-training probe — cosine similarity to ortho features at the GT pose, lifted-ground vs BEV-RGB — is blocked on the Durham orthophoto. Injection point when we get there: `f_q_pyramid[16]`, (B, 1024, 14, 14), a single dict in `matcher.py` (the backbone is single-scale; no fine CNN branch).
- 2026-09-19 — OxTS stored yaw is radians CCW from east, settled on data (`make oxts`, 11 241 moving frames): residual vs direction of travel mean +0.33°, σ 2.5°, 99.6 % within 10°; both clockwise readings give σ 124°. Confirms `oxts/dataformat.txt` and Dur360BEV's `map_api`; contradicts the brief's "heading NED, 0 = north, clockwise" — that is the RT3000 device convention, but the export re-expressed it. Bearing cw from north = 90° − yaw°; BNG grid convergence at Durham +0.35°. Modality sync is by frame index (16 407 timestamps each; image −1 ms, LiDAR +0 ms median vs OxTS, 30 ms worst case).
- 2026-09-19 — BEV attitude stays roll = pitch = 0 for task 01 (Maciej's call, after the cost was measured). OxTS exports no attitude (roll and pitch are identically 0 in all 16 407 files), and the LiDAR ground plane over 120 frames gives roll σ 1.31° / |max| 3.04°, pitch σ 0.58° / |max| 2.31°, which displaces the IPM by ρ²θ/h ≈ 5 m at the 28 m ring for a typical 1.3°. Known consequence: the outer ring of the query is metrically wrong, so H1 failures cannot be attributed to the matcher alone without re-testing with a fitted ground plane. Fitting that plane from the already-segmented ground returns remains the cheap fallback if H1 fails.
- 2026-09-19 — Sat-RoMa checkpoint for H1 is `0t1q66hy` (Maciej), superseding `4adxis71`. Architecturally identical — 224 px query, 896 px reference, map_to_img_ratio 4, single decoder scale 16, proj_dim 512 — so the switch is one config line. Differences: trained for aerial cross-scale search (reference edge 500–1500 m, query:reference 1:2–1:8, ±30° aircraft pitch/roll, 24 M samples) rather than planar poznan4 drone-in-preloaded-map (10 M samples), and its config is `verified` from the training sidecar whereas `4adxis71`'s was reconstructed (`assumed:no-sidecar`). Note the regime gap: our reference is 224 m at 0.25 m/px, 2–7× finer GSD than this checkpoint's training band. Smoke test passes on both; on the single synthetic pair yaw error is 0.03–0.09° for `0t1q66hy` vs 0.97–1.02° for `4adxis71`, corner/position within noise — one pair, not evidence.
- 2026-09-19 — New track `02_fusion` (Maciej): camera + LiDAR fusion BEV trained against Sat-RoMa. ERP → frozen DINOv3 ConvNeXt-T (LVD weights) + FPN → an image feature sampled for every LiDAR return ("point painting") + LiDAR channels → softmax-splat into the 224×224 / 0.25 m grid → residual BEV CNN with self-attention at 14×14 (layout after BEV-Patch-PF, re-implemented for a spherical camera and sparse LiDAR) → (B, 1024, 14, 14) fed as `f_q_pyramid[16]` to the released decoder; reference through the frozen `sat493m` ViT-L; loss = Sat-RoMa coarse cross-entropy over the 56×56 reference cells. This deliberately overrides three earlier entries and they stand superseded FOR THIS TRACK ONLY: "LiDAR is training-only" (here LiDAR is an inference input), "Lift-Splat is never the method", and "task 01 only until reported". It also resolves the 2026-09-19 feature-BEV objection by training the lifting module rather than using the frozen checkpoint zero-shot. Side effect: LiDAR places features metrically, so the missing roll/pitch costs cos θ here instead of ρ²θ/h. Coarse-target layout verified against the released checkpoint (`make targets`: row·K+col top-1 79.6 %, transposed 0 %).
- 2026-09-19 — Reference-map survey (no source chosen yet). Environment Agency VAP (OGL v3), queried through its WFS survey index: covers 100 % of the OxTS track including the full rotated 224 m reference, 22 tiles in 5 km squares NZ2535 / NZ2540, 25 cm — but the ONLY product is colour-infrared (`IRRGB`, 3 bands NIR-R-G, not true colour) flown 2009-07-25, 15 years before the drive; the nearest true-colour EA survey (2011, 10 cm) does not reach the track. Aerial Digimap (Getmapping, 25 cm true colour, GB-wide, latest 2022, BNG) is the best match and needs a UK-HE login (Durham collaborator); its EULA allows figures in papers with a copyright line but NOT publishing the data, so crops cannot be released. ESA Third Party Missions gives Pléiades Neo 30 cm (3.5 m CE90) free for research on an accepted proposal. Mapbox / Esri / Google / Bing tiles stay excluded (offline caching and extraction restricted by their terms).
- 2026-09-19 — CORRECTION to the reference-map survey above: Environment Agency VAP does NOT cover the track. The "100 % coverage" came from the EA WFS survey index, whose footprints are wrong for this survey. Downloaded the NZ2540 bundle (41 ECW tiles, 905 MB, read via the `allfab/gdal-ecw` Docker image): georeferenced from the index, the OxTS track crosses moorland and fields; at the positions embedded in the ECW headers the content is right (Josephine Butler College's circular campus at E427 N540, the Wear meander and Shincliffe at E428 N540) and the tiles turn out to be scattered 1 km river-corridor squares from E386 to E429 — 0 of 16 407 frames fall inside any of them. Also corrected: the product is true-colour RGB + alpha at 0.20 m, not colour-infrared at 0.25 m as the index says. Lesson kept as a rule: a reference map is accepted only after the OxTS track is drawn on it and follows the roads. EA is dropped; Aerial Digimap (Durham collaborator) is the path, ESA TPM Pléiades Neo the parallel application. Bonus found on the same portal: EA National LIDAR Programme 2021, 1 m DSM/DTM/intensity, does list NZ2540 — unverified, and an aerial-LiDAR height map is a different reference modality, not an orthophoto.
- 2026-09-20 — Downloaded the 4 EA bundles neighbouring NZ2540 (NZ2040/2535/2545/3040, ~2.1 GB) after the coverage correction above. 122 tiles now on disk; read positions from each ECW's OWN embedded georeferencing (confirmed correct, unlike the survey index) via `gdalinfo`/`gdal_translate` through the `allfab/gdal-ecw` Docker image. Result: 38.1% of frames (6248/16407) now fall inside a real tile, concentrated in the Durham city-centre loop (tiles P00027473/74/75/78/79 and P00027757). Converted those 6 to full-res GeoTIFF (RGB only — the "IRRGB" product's 4th band is a constant-255 alpha, not NIR, despite the product name) and built `data/ortho/durham/2009_ea_irrgb/durham_2009_25cm.vrt` (3x3 km, 0.2 m/px, EPSG:27700). Wired as `data.ortho`; still PARTIAL (36% of the smoke-test subset's 1911 frames have a fully-covered 224 m query footprint) — this is a real but incomplete reference, not a substitute for Digimap.
- 2026-09-20 — `OrthoMap.render`'s `valid` mask is geometric only (inside the raster's bounding rectangle), so a mosaic with gaps between tiles reads those gaps as black and still calls them valid — silently supervising the coarse loss against no data. Added `bevloc.data.pairs.fully_covered_names` (probes real pixel content, not just raster extent) rather than changing `render`'s contract, which other code (tests, `sample_reference`) depends on. `train_fusion.py` now filters to fully-covered frames before the train/val split; val range moved to [[710, 2400]] (inside the covered corridor; the old [[6000, 7500]] has no coverage at all).
- 2026-09-20 — First real (non-synthetic-reference) fusion training run, 300 steps / 520 covered frames / EA 2009 imagery: train loss 8.05 (chance) -> 6.5-6.7, argmax distance 17 -> 8-9 cells; val stayed near chance (loss 7.36, dist 12.7-12.9 cells). Expected shape for this little data/steps — real learning signal on train, no generalization yet. NOT evidence for or against H1/the fusion approach; it only confirms the training path is correct on genuine imagery. Checkpoint: `checkpoints/02_fusion_real_ea2009_run1.pt`.
- 2026-09-20 — Found and fixed a real bug (not a decision, logged for the record): the initial real-data run hung indefinitely with `DataLoader(num_workers=4)`, CPU near-idle, no error. Cause: CUDA is initialized in the main process (models built with `.to(dev)`) before the loader's workers are first spawned; forking a process with a live CUDA context reliably hangs rather than erroring. Fixed by setting `num_workers=0` — `FusionPairs.__getitem__` measured at ~0.1 s (well under one training step's ~1.7 s), so multiprocessing bought nothing here anyway. General lesson, not specific to this repo.
- 2026-09-20 — Debugged the EA VAP coverage gap end to end after Maciej's challenge that it looked like a download bug, not a real data gap. Two concrete checks: (1) re-fetched the NZ2540 zip and byte-compared all 41 files against disk — 0 mismatches, ruling out silent overwrite corruption from merging 5 bundles into one `ecw/` folder; (2) a 25x25 km wide search found TWO VAP bundles never tried (NZ2550, NZ3045) — a real gap in the original neighbour search, now fixed. Downloaded both (137 tiles total, up from 122); the 3 missing 1 km squares (E427-429, N541-543) are still absent. A search polygon drawn tightly around just those 3 squares names exactly one bundle as responsible (NZ2540, already verified complete). Conclusion: genuine hole in the 2009 flight plan (VAP follows river corridors; this patch sits off the river), not a download defect.
- 2026-09-20 — Checked every product/year in the EA catalogue within 25x25 km of Durham for a later optical survey (Maciej's request). Result: NO optical aerial photography of Durham newer than 2011 exists in this archive, and that one 2011 survey (NZ3030/NZ3035, true RGB) sits southeast of the drive corridor, not overlapping it. LiDAR products (point cloud, DSM, DTM, National LIDAR Programme) continue through 2024; optical photography stops at 2009-2011. 2009 IRRGB is therefore not an arbitrary choice but the newest optical imagery available for this area from EA.
- 2026-09-20 — Reference map for the missing 62% of the drive: National LIDAR Programme 2021 intensity (systematic nationwide survey, unlike VAP's river-selective flights). One seamless 5x5 km GeoTIFF (`DSM`/`DTM`/`Intensity_LZ_NZ2540_..._20211120.tif`), EPSG:27700, 1 m/px, float32 raw (12-bit-ish DN, NOT 0-255 -- an early conversion attempt assumed an 0-200 range and clipped nearly everything to white; fixed with a 0.5-99.5 percentile stretch to uint8, logged range 9.0-166.0). Checked pixel-by-pixel at all 16407 OxTS points: 99.9% real data, including 99.9%/100% inside the two former EA gaps. Converted file: `data/ortho/durham/2021_nlp/durham_2021_nlp_intensity_1m_uint8.tif`.
- 2026-09-20 — The raw intensity raster has ~86000 small nodata blobs (1.56% of area) from LiDAR no-return on water/specular surfaces: 98.5% of blobs by COUNT are <=10 px (speckle), but 20 blobs >2000 px hold 52% of the nodata AREA (the river and real water bodies). Filled blobs <=500 px with `cv2.INPAINT_TELEA` (speckle, not real gaps); left larger blobs as genuine nodata (water is legitimately hard to match in any modality, camera included). Residual nodata after fill: 0.89%.
- 2026-09-20 — `fully_covered_names`'s min_frac needs to differ by source: 0.999 is right for EA VAP (real gaps are large, hard-edged, missing-tile artifacts, so ANY leniency would silently supervise against no data); it is far too strict for the naturally speckled NLP raster even after gap-filling, since real water crossings inside an otherwise-good query box are legitimate content, not missing data (58% "covered" at 0.999 vs 95% at 0.90, with the difference concentrated near the river). Added `--min-coverage` to `train_fusion.py` (default 0.999) rather than changing the function's own default. Used 0.90 for NLP runs.
- 2026-09-20 — First real NLP-intensity fusion training run, 300 steps, 1689 covered frames (val: frames 6000-7500, inside the former EA gap A, min-coverage 0.90): train loss 8.05 (chance) -> ~7.0-7.2 (fluctuating), argmax distance ~20 -> ~10-11 cells; val loss 7.06, argmax distance 12.4 cells. Directionally similar to the EA-RGB run (which reached train loss 6.7, argmax 8.4 cells) but more modest in the same 300 steps -- expected, since this run trains over 3.2x more distinct frames (1689 vs 520) in the same step budget, not necessarily a domain-fit failure of feeding LiDAR intensity to the RGB-trained `sat493m` backbone. Not enough steps to separate "needs more training" from "the domain gap actually hurts"; a longer run is needed before drawing a conclusion either way. Checkpoint: `checkpoints/02_fusion_real_nlp2021_run1.pt`.
- 2026-09-20 — Patch-level black-reference masking (Maciej: "we can use black ref map"), replacing the whole-frame strict gate. `bevloc.model.coarse.ref_cell_validity` flags individual 56x56 reference cells as black/no-data; `coarse_targets` now takes an optional `ref_valid` and excludes only the query patches whose target lands on one, not the entire frame. `fully_covered_names` is kept only as a cheap existence pre-filter (`--min-coverage`, default now 0.05: "has some overlap at all", not "is fully clean"). Effect: NLP training frames went 1689 -> 1760 (now ~100% of the downloaded 1911-frame subset, vs 58-95% under the old strict whole-frame gate depending on threshold). Unit-tested (a hole in one of 196 cells drops exactly that one patch).
- 2026-09-20 — Built `scripts/eval_fusion.py`: qualitative + quantitative check of a trained checkpoint on real frames, since RANSAC alone gives no signal at all in early training (it either finds a coherent pose or reports nothing). Shows, per frame: an IPM-RGB BEV panel (human orientation only, NOT the network's input), the reference with GT (green) vs RANSAC estimate (red) boxes, AND the raw per-patch argmax of all 196 query patches as points with a line to where each should land -- this stays informative long before RANSAC succeeds.
- 2026-09-20 — First qualitative look at the fusion pipeline (checkpoint at step 400, NLP intensity reference, patch masking): RANSAC failed on all 5 test frames (`use_means=False`'s default one-peak-per-patch RANSAC needs several geometrically spread agreeing points; see the 2026-09-19 decision on this default). But the raw per-patch argmax median error is 26-80 m across the 5 frames, consistently well under the ~112 m chance level on the 896 px reference -- real, if weak, learned signal. Visual pattern: most of the 196 patches collapse onto nearly ONE point, close to but not exactly the GT location, rather than each patch predicting its own distinct correct cell -- a known early-training local minimum (predicting the likely neighbourhood minimizes average loss before the decoder is confident enough to differentiate individual patches), and the direct cause of the RANSAC failures: a degenerate single-point "vote" cannot fit a spread-requiring similarity transform. Not evidence the approach fails; 400 steps is very little for a decoder that needs to learn per-patch differentiation. Next real checkpoint of this run should be trained substantially longer before drawing a conclusion.
- 2026-09-20 — Multi-frame sequence queries, for "does denser LiDAR help" (Maciej). Added `PointPaintSplat.forward_multiframe` / `FusionBEV.forward_sequence`: each source frame samples its OWN image features from its OWN camera-relative points (`xyz_cam`), but places them in the BEV grid via the SAME points transformed into the query frame's level frame by the relative odometry pose (`xyz_bev`, `se2_from_pose`, same convention as `bevloc.bev.mosaic`) -- all frames then splat into one shared grid, reusing the existing softmax-pooling. Unit-tested: 1-frame-at-identity reduces exactly to the existing single-frame path; a second frame's point is verified to land in the BEV cell implied by its transformed position while still sampling from its own (distinguishable) image feature. `scripts/eval_fusion.py --seq-dists` builds queries from 0/2/5/10 m of odometry-accumulated driving (KISS-ICP poses, forward-looking from the query frame, same direction as `make_mosaic.py`).
- 2026-09-20 — Result on the step-400 NLP checkpoint (3 query frames, 130-299 KISS-ICP range): sequence length made NO measurable difference (frame 200: argmax error 22.4/22.6/22.2/22.1 m at +0/2/5/10 m, up to 11 frames/9.9 m accumulated; frame 140 and 180 equally flat). Consistent with, and further evidence for, the 2026-09-20 mode-collapse finding: the network's output is currently near input-independent (most patches vote the same rough location regardless of content), so adding more LiDAR density cannot change a prediction that is not yet reading fine BEV content in the first place. This is not evidence sequences don't help -- it is evidence the checkpoint needs substantially more training before this question is answerable at all.
- 2026-09-20 — 2000-step joint training run (NLP intensity, patch masking, 1760/32 train/val frames): train loss 8.05 (chance) -> 5.5-6.1 by step 1750-2000 (best 5.54), argmax distance 19 -> 8-10 cells, top-1 0 -> ~2%; val loss improved only to step ~500-750 (7.06 -> 6.73) then plateaued at 6.70-6.86, and val argmax distance got WORSE from step 250 to 2000 (9.75 -> 13.1 cells) while train kept improving -- classic overfitting signature. Checkpoint: `checkpoints/02_fusion_real_nlp2021_run3_2000steps.pt`.
- 2026-09-20 — Qualitative re-check on this checkpoint (8 frames, 3 train-side + 5 val-side): RANSAC now succeeds on 7/8 (was 0/5 at step 400) -- the mode-collapse pattern (all 196 patches voting nearly the same point) is gone, replaced by a genuinely spread, geometrically-consistent set of per-patch votes. But absolute accuracy remains poor and inconsistent: position error 6.7-96.1 m (mean ~42 m across the 7 successes), yaw error 4.3-124.0 deg (one essentially a wrong rotational hypothesis), train-side frames averaging better than val-side (position ~15 m vs ~63 m), consistent with the overfitting signal above. Conclusion: joint training of the BEV encoder and the Sat-RoMa decoder is the right mechanism (confirmed -- it moved the pipeline from degenerate to structurally working), but the bottleneck has now shifted from "not enough training steps" to "not enough training DATA diversity" -- 1760 frames from one ~86 m odometry-covered stretch, repeated many times, is not enough to generalize. More steps on this same data are expected to widen the train/val gap further, not close it.
- 2026-09-20 — Built `scripts/viz_features.py`: PCA(3)-to-RGB of the raw pre-encoder BEV grid (own colour scale) and of the query tokens f_q vs reference tokens (ONE PCA fit jointly on both, so colours are directly comparable). Located the mode-collapse finding one level deeper: the raw BEV grid (post lift/splat, pre `BevEncoder`) shows real spatial structure matching the LiDAR content (object clusters, road), but the query tokens f_q -- what the decoder actually receives -- are almost perfectly flat/uniform across the 14x14 grid on the step-2000 NLP checkpoint, while the reference tokens (frozen, mature DINOv3 ViT-L) show rich spatial texture on the identical colour scale. This is the direct, one-level-deeper cause of the gm_cls mode collapse: the decoder cannot discriminate where a query patch belongs when the query tokens barely differ from each other in the first place. Points at `BevEncoder` (the small CNN + self-attention compressing 224x224 -> 14x14 into the matcher's feature space) as not yet having learned to encode spatial position, rather than (only) a decoder-training deficit. Worth re-checking this same panel after substantially longer training: if it stays flat, that points to something structural (encoder capacity, its positional embedding, or the coarse loss giving too weak a signal to force spatial differentiation) rather than simply needing more steps.
- 2026-09-20 — CORRECTION of the entry above ("query tokens are almost perfectly flat"): that was a visualization artefact, not a property of the tokens. Measured on 3 frames: trained query tokens have mean pairwise cosine 0.44-0.48 and token-to-token variation 0.71-0.74 of their norm, i.e. 4x MORE diverse than the reference tokens (cosine 0.97, variation 0.18); the joint PCA spent its axes on the offset between the two clouds (5.2-5.6 reference-spreads apart) and the percentile clip painted all 196 query tokens one colour. `viz_features.py` now uses one PCA per panel. What the corrected picture shows instead: (1) the query tokens vary as a smooth gradient across the grid -- the encoder's positional embedding -- with no visible scene content, whereas the reference tokens trace roads and buildings; (2) their statistics are nowhere near what the decoder was trained on: real `sat493m` tokens are a large shared component plus a small informative deviation (cosine 0.94-0.97 even for the IPM-RGB BEV pushed through sat493m), ours are LayerNorm'ed near-orthogonal vectors, and 2000 steps barely moved that (0.45 -> 0.47); (3) the BEV input is starved: LiDAR returns reach only 15-17 % of the 224x224 cells and none on the road (wet asphalt), while the IPM ground that the camera does see covers 19-46 % and is not used by the fusion lift at all (Maciej's observation).
- 2026-09-20 — Two fixes to the fusion track (Maciej approved): (1) GROUND FILL in `PointPaintSplat`: virtual points at BEV cell centres on z = −lidar_height, kept only in front of the first above-ground LiDAR return of their azimuth bin (the LiDAR's own occlusion boundary, −0.5 m) and where the ERP is not ego/seam; they sample image features like any point and carry an `is_lidar = 0` channel (LIDAR_CHANNELS 4 → 5, old checkpoints no longer load). Feature-space version of `oracle_b`; roll = pitch = 0 as elsewhere. In multi-frame queries a frame's ground follows its relative pose (`to_bev`). (2) TOKEN ALIGNMENT LOSS `alignment_loss`: each query token is regressed onto the frozen `sat493m` reference token bilinearly sampled at its GT location — MSE in units of the reference's token spread + cosine on mean-removed tokens (plain cosine is satisfied by the shared mean alone; unit-tested). `train.align_weight = 1.0`, added to the coarse CE. This is the plain-regression form of the kick-off's "alignment on pre-decoder features" (§3.4 ii); the contrastive, orientation-aware version (H5) is still the planned contribution, not this.
- 2026-09-20 — Kick-off document re-read against the current state (Maciej's request). Still matching: the problem, Sat-RoMa multi-hypothesis matching on a 4× reference, the losses (coarse CE + pre-decoder alignment), Dur360BEV, route-disjoint validation, the metrics. Deliberately diverged, as a feasibility probe only: LiDAR is an inference input and the lift is learned point-painting + a BEV CNN (the kick-off's H4 "learned lifting" baseline family, BEV-Patch-PF layout) instead of geometric IPM + contact line + geometry head; attitude is ignored (no IMU data in the export); the reference is NLP LiDAR intensity at 1 m instead of a 10–50 cm orthophoto. Not yet touched: H1 exactly as written (zero-training oracle BEV on the unchanged matcher against a real orthophoto) — blocked on imagery that covers the drive; the geometry head (H3), contrastive negatives (H5), SE(2) 2-point solver (H7), particle filter (H6). "EDM" in the kick-off's references is Jung et al.'s ERP↔ERP dense matcher; it is not used anywhere in the pipeline (we never match two panoramas) — its transferable idea is a spherical positional encoding for ERP features.
- 2026-09-21 — Reverted the token alignment loss (Maciej: "drop the alignment-loss commit") after the run-4 result above: `alignment_loss`, `gt_ref_xy` in `bevloc.model.coarse`, `train.align_weight`, and their wiring in `train_fusion.py`/`FeatureQueryMatcher.forward` (`return_ref`) are removed; the unit test is removed. Ground fill (the other half of run 4) is KEPT — it has its own passing test and no evidence against it; only the alignment loss was implicated by the flat align_cos + worse train/val numbers. `experiments/02_fusion/real_nlp2021_run4_ground_align/` (config + log, no checkpoint) is left in place as the record of what was tried; the code that produced it no longer exists, so it is not reproducible as-is. Re-attempt alignment only with a much lower weight, a warm-up, or a reformulated loss — not by restoring this version.
- 2026-09-22 — Mapillary sequence `irAsBUKtGCfhPHuMbmOcLd` (Fixtor, 4.3 km, 1309 frames inside Poznań) is a held-out test sequence. Do not train on it.
- 2026-09-22 — New track `05_lift_splat` (Maciej): camera-only spherical Lift-Splat trained with Sat-RoMa coarse CE on Mapillary 360 + Poznań Geoportal orthophotos. ERP (2:1) → frozen `sat493m` ViT-L → soft depth over 16 bins along each ray → splat into 224×224 / 0.25 m → thin strided CNN to (B, 1024, 14, 14) as `f_q_pyramid[16]`; reference through the same frozen encoder; loss = RoMa coarse CE (+ certainty) inside a local window; black / missing ortho crop raises (no match). No LiDAR. Differs from `02_fusion` by (1) encoding the panorama with `sat493m` rather than ConvNeXt-T+BevEncoder, (2) learned depth instead of LiDAR placement, (3) a thin BEV head instead of the residual+attention tower that flattened fusion tokens. First data = Fixtor `gXabFhpwk2dcl0i4518mDQ` vs year_2025 tiles `e357585_n505324` and `e359710_n505264`. Overrides "Lift-Splat is never the method" and "no dense depth network" FOR THIS TRACK ONLY.
- 2026-09-22 — Lift-Splat multi-seq train set (Maciej): Fixtor `iHfmEq03Tc6752Y4Ke8wlC`, `NWVA14Y83pMRsijaGFkmQS`, `gXabFhpwk2dcl0i4518mDQ`, `doQ3OhJBKe56c8UxjAFmat` (frames filtered to sit 120 m inside a 2025 Poznań tile). Held-out val route: `IcRzj0wTLZX874qitxVsQa` (route-disjoint). `irAsBUKtGCfhPHuMbmOcLd` remains reserved when downloaded. Local window: offset 0.10 / rot 10° / CE radius 6.
- 2026-09-22 — Lift-Splat GO (pose, not argmax): on `fixtor_multi_best` after 2000 steps, held-out IcRzj n=48, local window. RANSAC peak median pose 14.3 m @2025 (recall@5/10 m = 12.5%/37.5%, 48/48 matched); GMM means 15.6 m. Same checkpoint zero-shot on 2024 orthophoto: peak 11.2 m, means 9.7 m (2024 GSD 5 cm vs 2025 25 cm). Argmax alone was 17.6 m — understates. Primary metric from here = RANSAC pose. Continue with cross-year refs + neighbour hinge overnight; do not treat global search or 100 km more data as the next step.
- 2026-09-23 — Lift-Splat training augmentations (Maciej): per-sample random reference scale ∈ {3,4,5,6} (same 0.25 m GSD; matches `0t1q66hy` 1:2–1:8 band), random pose window offset_frac ∈ [0.05,0.18] and rot ∈ [3°,20°], colour jitter on ERP+ref, ±2° attitude noise on `computed_rotation`, plus existing cross-year year sampling. Val stays fixed scale-4 / ±22 m / ±10°. Batch must remain 1 when scales mix. **Coarse targets always use K = √(gm_cls channels) = 56** (decoder emits a fixed 56×56 classification grid regardless of ref pixel size; cell GSD scales with the crop: 3 m / 4 m / 5 m / 6 m at scales 3–6). An earlier `cells = ref_px/16` wiring was wrong and OOB'd CE on scale>4 — fixed 2026-09-23.
- 2026-09-23 — No-match negatives in Lift-Splat train (Maciej): `neg_frac=0.15` samples displace the reference by `neg_sep_frac ∈ [0.70,1.40]`×ref edge so the GT pose is off the crop (real ortho, wrong place). Sat-RoMa `gm_cls` has **no unmatched class bin** — CE is skipped for that sample and `gm_certainty` is trained toward 0 (RoMa `ce_weight=0.01`). Val stays all-positive.
- 2026-09-23 — Tonight deep-dive = **A pose-heatmap NLL** + **B multi-frame Mapillary splat** (kickoff §8): (A) certainty-weighted soft vote of `gm_cls` → local NLL of GT vehicle cell, λ=0.5; (B) soft-splat frames at 0/2/5 m behind query via GPS+compass SE(2) into one BEV (`forward_multiframe`). Schedule: aug → A → B → RANSAC before 06:00.
- 2026-09-23 — Mapillary label framing (Maciej): **position** from `computed_geometry` is usable as a GT **proxy** for metre-level pose metrics. The green rectangle on orthophoto overlays is only an **approximation** of the query footprint (heading, attitude, flat-ground, GSD) — do not call it GT. Viz captions updated accordingly.
- 2026-09-23 — Next comparison task (Maciej): test whether **FG² and BevSplat transfer to Fixtor × Poznań**, using released VIGOR checkpoints first and minimal 2,000-update fine-tuning only if needed. Both consume one immutable ≥200-frame held-out manifest with the same local ±22 m / ±10° prior, 2025 primary and 2024 cross-year maps, Mapillary pose proxy, and method-native raster sizes/GSD. `third_party/` stays read-only. Gates: competitive = median ≤10 m and R@5 ≥25%; works-but-weaker = median ≤20 m and R@10 ≥30%. Full protocol: `docs/tasks/02_fg2_bevsplat.md`.
- 2026-09-23 — Evaluation protocol (task 03): train = the 4 Fixtor routes; validation/selection = IcRzj (`experiments/06_fg2_bevsplat/manifest.json`, 200 frames); TEST = irAsBUK (`manifest_test.json`, 200 frames, never used for training or checkpoint selection). Every table reports median, R@5 and R@10 with 95 % bootstrap intervals and a centre-guess chance row (median 17.9 m, R@5 4 %, R@10 16 % for the ±22.4 m window). Reason: the 8.7 m Lift-Splat result was n=48, the checkpoint was selected on the same route it was reported on, and 1–3 m differences between runs were inside noise. Eagle runs live on `storage_6` project_data (pl1269-01) because the `storage_5` Lustre scratch returned I/O errors on 2026-09-23.
- 2026-09-23 — Task 2 gate (docs/tasks/03 plan): the camera-only RGB-IPM query beats every learned lift. On the reserved test route (irAsBUK, 200 frames) IPM through the frozen `sat493m` encoder + fine-tuned decoder scores 4.6 m median [3.9, 6.5], R@5 0.53, R@10 0.67; the Lift-Splat variants (multi/years/aug/seq/pose_nll) and the hybrid lift score 14.9–17.5 m against 18.3 m chance. Decision: the learned depth-bin lift (`--query lift`, 05_lift_splat) is retired as the camera-only method and kept only as the H4 "learned lifting" ablation row; the camera-only baseline of record is `--query ipm`; the hybrid is reframed as "IPM + a treatment of above-horizon content", first candidate the ERP-token query (Task 6, after Loc², `docs/related/architecture_report_roma_losses.md` §6). Reason: BevSplat Tab. 3 predicted this ordering; our validation-route numbers had been flattered by selection, the test manifest was not. `experiments/05_lift_splat/REPORT.md`.
- 2026-09-23 — The "published" solver in every table so far is an 8-DoF homography, not sRT. Checked in `sat-roma-infer`: with `refine=False` (the paper configuration) `pipeline.estimate_homography(model="sRT")` returns the `cv2.findHomography` RANSAC init unchanged; the `model` argument only steers the disabled LM refinement. Visible in the overlays as sheared RANSAC boxes on the miss frames. Consequence: the H7 comparison (homography vs fixed-scale SE(2)) has never actually been run in this repo. `matcher.solver` now has three values: `srt` (package default = homography, kept as the reference row), `sim` (4-DoF similarity RANSAC via `ransac_init(estimator="similarity")`), `se2` (3-DoF, `bevloc.match.se2`). All three are evaluated on the IPM and ERP queries on both manifests before any of them becomes the default.
- 2026-09-24 — H7 (fixed-scale SE(2) beats homography) is not supported on the `ipm` query: homography / similarity / SE(2) give 4.6 / 4.5 / 4.5 m median on the test route with overlapping intervals and the same >30 m tail. `matcher.solver: srt` stays the default (reference row = published package configuration); `sim` and `se2` remain selectable. Kick-off §3.3's 49-vs-567-trials argument is about cost, not accuracy, and cost is not the bottleneck.
- 2026-09-24 — Task 3 (hybrid: dense IPM *features* + learned above-horizon splat) fails its gate: from scratch it is at chance on the test route (19.3 m), warm-started it inherits the lift's 15.9 m. Task 6 (ERP-token query, decoder-only fine-tune, 3000 steps) lands at 14.3 m, no better than the lifts. Both place ground-view *tokens*; the `ipm` query places ground-view *pixels* and encodes the resulting overhead-looking picture (4.6 m). Working rule from now on: the frozen `sat493m` encoder must see an overhead-looking image; feature-level placement needs a trained cross-view head, which is a separate design decision.
- 2026-09-24 — Task 5 gate met with a caveat: the SE(2) particle filter with the `ipm` query's vote heatmap as observation tracks the whole reserved route (1265 frames, 4.3 km) at 4.4 m median / 21.6 m p95, 83 % of frames within 10 m, prior alone 4.6 m; per-frame RANSAC on the same crops 4.5 m / 26.3 m. Caveat: motion = Mapillary proxy relative pose + 1 m / 1° noise, not real odometry; errors vs the proxy. The filter helps the tail, not the median; runs of consistently wrong observations (20–50 frames) are followed before recovery. Likelihood flattening (BEV-Patch-PF Eq. 5) is the next filter-side lever. `experiments/07_track/REPORT.md`.
- 2026-09-24 — Next steps after the task-03 results (Claude's call; Maciej delegated "decide and go on"). Diagnosis on the `ipm` test evaluation: 67/200 frames miss by >10 m, 73 % of them miss in 2024 as well (frame-specific, not map-appearance), median yaw error on misses 5.8° (not a rotation failure), inlier ratio 0.80 vs 0.95 for hits (a weak gate: keeping inliers ≥ 0.9 retains 64 % of frames with 18 % still >10 m); validation CE still falling at step 3000 (best checkpoint at 2400). Decided, in order: (1) continue the IPM training to 10 000 steps (`ipm_long`, queued) because it is undertrained, not overfit; (2) multi-frame IPM mosaic query (IPM pictures of the frames 2 and 5 m behind, warped by the proxy relative pose, nearest-range compositing; the existing `seq_dists_m` plumbing) — the tail is along-road ambiguity and the picture is mostly road, so more road per query is the direct remedy; (3) camera-only contact-line IPM for Mapillary (`ipm_cl`, kick-off H2) with the contact row from a frozen semantic segmenter instead of LiDAR — this adds above-ground structure to the picture without a lift; it introduces a segmentation model, which is the design change; (4) not pursued now: more solvers, more losses, feature-level lifts, the ERP-token query without a trained head. Reference-side work (cross-year, OSM channel) waits until (2)–(3) are measured.
- 2026-09-24 — Mapillary camera height `ipm.height_m = 1.7 m` is an ASSUMPTION (flagged by code review): Fixtor's rig is a car-mounted 360° camera and the files carry no height; 1.7 m is a typical roof-mount value, uncertainty ±0.3 m (18 % of scale at 2.0 m). It fixes the metric scale of the IPM picture and of the fixed-scale `se2` solver. Evidence it is close: the 4-DoF `sim` solver, which absorbs a global scale, gives the same medians as `se2` (4.5 vs 4.5 m on the test route). To do: log the similarity solver's estimated scale per frame in `eval_pose` and fit the height from its median over a manifest (`h_true = h_assumed / s`); until then the value stands. Durham's 1.84 m is unrelated (different vehicle).
- 2026-09-24 — Code review of the task-03 branch (10 findings, `experiments/05_lift_splat/VERDICT.md` for the list): fixed the `sim` solver's silent identity-on-failure, NaN bootstrap intervals under heavy misses, dead code and hard-coded scale in `eval_pose`, duplicate neighbour decoding in the IPM mosaic, `--query` override crash, `track_route` on ERP checkpoints; evaluation scripts now decode once and run consensus for both RANSAC rows on the same logits. Not fixed on purpose: `HybridQuery`'s per-step CPU sync (the hybrid is retired).
- 2026-09-24 — Query-side work is closed for now. After the task-03 gates: plain camera-only IPM picture + Sat-RoMa = 4.3–4.6 m median / R@5 0.53–0.55 / R@10 0.67–0.70 on the reserved route; 10k steps, multi-frame mosaic (proxy-pose warped), ego-body cut, semantic dynamic mask, contact-line pictures (three variants), hybrid feature lift and ERP-token query all score inside that interval or below (`experiments/05_lift_splat/REPORT.md`, VERDICT.md). Decided: (1) `--query ipm` with `configs/ipm_v1.yaml` settings stays the method of record; the trained mosaic is kept as the "sequence" row (R@10 +4–5 points, same median); (2) the next experiments are on the reference side and the filter, in this order: (a) leaf-off / other-season Poznań imagery as an additional reference year at train and test (the misses are tree-lined roads under canopy), (b) an OSM road/building raster as an auxiliary reference channel (report §5 idea 3), (c) BEV-Patch-PF-style likelihood flattening in the particle filter and real odometry for the mosaic; (3) not pursued: further picture variants, solvers, losses. OPEN item (6) (trained query-side head) stays open for Maciej.
- 2026-09-24 — Leaf-off reference imagery does not help (evaluated, not retrained): 2019–2022 Poznań flights are leaf-off; the 10k IPM checkpoint scores 4.7 m / R@5 0.55 against 2022 and 4.3 m / 0.55 against leaf-on 2025 on the test route, same tail. The misses on tree-lined road are along-road ambiguity of the road picture, not canopy in the map. Reference-side item (a) of the earlier entry is closed; (b) OSM channel and (c) filter tail remain, and I recommend (c) first because the filter already turns the same observations into 4.4 m along the route and its failure runs are what the flattening addresses. Manifests carry 2022/2021 entries next to 2025/2024 for cross-season scoring of any checkpoint.
- 2026-09-24 — VIGOR Chicago same-area, known orientation, 3000 test samples (`experiments/09_vigor/`): zero-shot IPM checkpoint (Poznań-trained) 5.59 m median / 10.07 m mean / R@5 0.45; after 3000-step decoder fine-tune on 12,740 Chicago panoramas (batch 4, Poznań recipe) 3.61 m median (CI 3.47–3.78) / 6.35 m mean / R@5 0.62 / R@10 0.80; centre-guess chance 14.3 m. Same-area only: train and test tiles come from the same city, and published FG²/Loc² numbers (1.4–1.9 m median) are not like-for-like (they train on more data and tune longer). Cross-area still blocked on four refused Drive files. Not yet tried: longer training, unfreezing more than the decoder.
- 2026-09-24 — VIGOR Chicago same-area, 30k-step decoder fine-tune (same recipe, ~9 epochs, job 8755888): 2.90 m median (CI 2.79–3.00) / 5.45 m mean / R@5 0.70 / R@10 0.85 on the same 3000 test samples, vs 3.61 m after 3k steps and 5.59 m zero-shot. Validation CE plateaued at ~2.35 from step 15k and drifted up after 20k (overfitting); the saved checkpoint is step 14.5k (best validation pose proxy 5.3 m). Longer training at this LR is exhausted; the remaining gap to FG²/Loc² (1.4–1.9 m same-area) is not a step-count problem. Next: FG² on the identical 3000 samples (like-for-like), no-Poznań-warm-start ablation, cross-area when the data arrives.
- 2026-09-24 — FG² like-for-like on VIGOR Chicago same-area (`scripts/eval_fg2_vigor.py`, job 8763035): the authors' released same-area known-orientation checkpoint, native 714×1428 / 630×630 inputs, third_party/FG2 unchanged (its two mmcv layers replaced by `bevloc.baselines.mmcv_shim`, pure PyTorch, strict checkpoint load), on exactly the 3000 test panoramas our rows use: weighted-Procrustes (paper default) 1.13 m median (CI 1.09–1.16) / 2.12 m mean / R@5 0.91 / R@10 0.96; RANSAC option 1.06 m median / 1.86 m mean / R@5 0.95. Ours after the 30k decoder fine-tune: 2.90 m / 5.45 m / 0.70 / 0.85. FG² is 2.6× better in median and 2.6× in mean on identical samples; the earlier "published numbers are not like-for-like" caveat no longer applies. Gap analysis is the next question: FG² trains a query-side lift (learned height selection) and matches at ≈1.7 m cells over 71 m; ours keeps a frozen encoder on a flat-ground picture and classifies 4 m cells. Same-area only; cross-area FG² checkpoint is staged for when the data arrives.
- 2026-09-25 — FG² cross-area like-for-like (released cross-area known-orientation checkpoint, job 8765488) on 6000 San Francisco + Chicago test panoramas drawn as eval_vigor.py draws them (seed 0): RANSAC option 1.41 m median (CI 1.38–1.43) / 2.45 m mean / R@5 0.93 / R@10 0.96 (paper Tab. 1: 1.37 / 2.41, so the shim reproduces the checkpoint on this split too); weighted Procrustes 1.60 m / 2.80 m / 0.88. Per city, RANSAC: San Francisco 1.34 m / 2.06 m, Chicago 1.48 m / 2.88 m; centre-guess chance 14.85 m. Our cross-area rows (zero-shot Poznań checkpoint, and the New York + Seattle fine-tune, job 8765486) are queued against the same 6000 samples.
- 2026-09-25 — Transfer of the Chicago-only 30k fine-tune to the other cities (same-area test, 12000 samples across four cities, job 8765490): all 3.89 m median / 6.63 m mean / R@5 0.60; Chicago 2.98 m (the trained city, consistent with the 2.90 m 3000-sample row), Seattle 3.46 m, San Francisco 3.67 m, New York 5.66 m (zero-shot Chicago was 5.59 m, so New York gains almost nothing from Chicago training). The decoder learns city-general matching to a degree, but the four-city run (job 8765483) is the proper same-area row.
- 2026-09-25 — No-Poznań ablation (job 8763046/8763047): the same 30k-step Chicago recipe started from the released Sat-RoMa decoder instead of the Poznań IPM checkpoint scores 2.90 m median (CI 2.80–3.02) / 5.44 m mean / R@5 0.71 / R@10 0.85 on the 3000-sample set, identical to the warm-started run (2.90 / 5.45 / 0.70 / 0.85); the validation curves coincided within noise from step 6k on and plateaued/overfit at the same steps. Conclusion: on VIGOR the Poznań pre-training contributes nothing measurable; the decoder learns the flat-ground-picture matching from VIGOR alone, so the 2.9 m plateau is the architecture's, not the data's. The Poznań checkpoint remains the warm start for the four-city and cross-area runs for consistency only.
- 2026-09-25 — Agreed (Maciej): next task = Loc² geometry with the Sat-RoMa decoder as the matcher (`docs/tasks/04_loc2_matcher.md`). Reasoning from the papers, not from memory: Loc²'s correspondence loss (2509.09792v3 App. H, Eq. 22–23) is a cross-entropy over aerial points with the pose-projected positive, the same form as RoMa's regression-by-classification (2305.15404v2 §3.3–3.4), so swapping the loss alone is a no-op; RoMa's gains came from the Transformer match decoder (its Tab. 2), the loss form is worth ≈10 % relative and the robust fine stage a little more. Loc²'s Tab. 12 (BEV-plane matching 8.20 m vs image-plane 1.75 m median) explains our IPM-picture floor independently of the loss; its Tab. 11 shows the cosine temperature is a sensitive hand-set knob a learned decoder replaces; its Tab. 15 warns that CE with pose-derived positives can hurt under unknown orientation. Plan: (1) Loc² baseline rows on our draws (released checkpoints, UniK3D depth, `make loc2-depth` / `make loc2-vigor`); (2) ERP-token query with depth placement + Sat-RoMa decoder + VCE pose loss (+ optional projection head, OPEN 6), fast 10k-step Chicago check against 2.90 m; (3) ablations: CE on cosine logits (control), no head, fine offset head. Loc² is AGPL-3.0 and FG² GPL-3.0: wrapped as baselines, never copied into src/.
- 2026-09-25 — Validation protocol aligned with FG²/Loc² (`train_vigor.py --val-frac 0.2`, default): checkpoint selection on a held-out 20 % of the training list (seed 0), never on test labels. The four-city (8765483) and cross-area (8765486) runs of 24–25 Sep selected on a 400-sample draw from the test list and are superseded by the v2 reruns (8777838, 8777826) once those land.
- 2026-09-25 — Four-city same-area decoder fine-tune (52,609 panoramas, 60k steps, batch 8, job 8765483; checkpoint selection on a 400-sample test draw, superseded by the v2 rerun) on the 3000 Chicago samples: 2.51 m median (CI 2.42–2.60) / 4.95 m mean / R@5 0.76 / R@10 0.87, vs 2.90 / 5.45 / 0.70 Chicago-only. Four times the data and twice the schedule buy 0.4 m; FG² is at 1.06 m on the same samples. Data closes a fraction of the gap; the rest is the architecture (Task 04).
- 2026-09-25 — Solver ablation on the Chicago 30k checkpoint, same 3000 samples: homography (`srt`, default) 2.90 m / 5.45 m / R@5 0.70; 4-DoF similarity (`sim`) 2.91 / 5.50 / 0.71; 3-DoF fixed-scale (`se2`) 2.95 / 5.44 / 0.70. The sheared quadrilaterals on the worst-20 sheet (`experiments/09_vigor/viz/vigor_chicago30k_worst20.jpg`) are a symptom: the 31–47 m misses come from diffuse vote maps centred on the tile (a layout prior) and land in the same place under every solver. The tail is a matching failure, not a solver failure; the solver stays `srt`.
- 2026-09-25 — Cross-area decoder fine-tune (New York + Seattle, 51,520 panoramas, 60k steps, batch 8, job 8765486; checkpoint selected on a 400-sample draw from the TEST cities — indicative only, the v2 rerun with held-out validation supersedes it) on the 6000 SF + Chicago samples: 3.27 m median (CI 3.17–3.34) / 5.61 m mean / R@5 0.69 / R@10 0.86; San Francisco 3.43 m, Chicago 3.05 m. Zero-shot Poznań checkpoint 6.09 m; FG² 1.41 m on the same samples. Cross-city generalisation of the decoder is good (Chicago unseen: 3.05 m vs 2.51 m when trained on it); the gap to FG² is the same factor 2.3–2.4 as same-area.
- 2026-09-25 — Loc² like-for-like, same-area (released same-area known-orientation checkpoint, UniK3D metric depth computed with Loc²'s own preprocessing, job 8778236) on the 3000 Chicago samples: RANSAC (paper setting on VIGOR) 1.33 m median (CI 1.29–1.37) / 2.93 m mean / R@5 0.88 / R@10 0.93; weighted Procrustes without RANSAC 1.70 m / 3.24 m. Paper Tab. 2 (all four cities): 1.59 m / 3.06 m, so Chicago is a slightly easier city for it and the pipeline (depth + mmcv shim + wrapper) reproduces the checkpoint; Task 04 Step 1 gate passed. Same samples: FG² 1.06 m, ours 2.51 m (four-city) / 2.90 m (Chicago-only). Cross-area Loc² row queued behind the cross-area depth job.
- 2026-09-25 — Four-city same-area checkpoint on 12,000 test samples across the four cities (job 8765485): all 2.95 m median (CI 2.90–3.01) / 5.16 m mean / R@5 0.71 / R@10 0.87; Seattle 2.41 m, Chicago 2.60 m, San Francisco 2.78 m, New York 4.18 m (was 5.66 m with Chicago-only training: the biggest gain, still the hardest city). Published same-area, all cities: FG² 1.08 m median / 1.95 m mean, Loc² 1.59 m / 3.06 m. Our four-city row is the comparable one for the paper table (v2 rerun with held-out validation pending).
- 2026-09-25 — Loc² like-for-like, cross-area (released cross-area known-orientation checkpoint, job 8778237) on the 6000 SF + Chicago samples (5999 scored: one pose non-finite): RANSAC 1.99 m median (CI 1.94–2.03) / 3.65 m mean / R@5 0.87 / R@10 0.93; Procrustes 2.21 m / 3.67 m; San Francisco 1.66 m, Chicago 2.32 m (RANSAC). Paper Tab. 2: 1.90 m / 3.43 m — reproduced within 0.1 m. Cross-area table on identical samples: FG² 1.41 m, Loc² 1.99 m, ours 3.27 m (NY+Seattle fine-tune), ours zero-shot 6.09 m; centre guess 14.85 m.
- 2026-09-25 — Task 04 fast check (Step 3): `--query erp_depth --head`, Chicago same-area, 10k steps, batch 4, held-out validation (jobs 8778958/8778959 + evals 8778960–8778964), 3000-sample draw: best checkpoint (step 8500) 3.80 m median / 6.23 m mean (homography), 3.71 m / 5.91 m (se2 solver); last checkpoint 3.69 m / 6.45 m; the control WITH the heat-map pose loss 0.5 (best, step 9500): 3.52 m / 6.88 m (homography), 3.45 m (CI 3.30–3.58) / 5.76 m / R@5 0.66 (se2). Gate (< 2.90 m) not met at this budget, but the gate was not like-for-like: the IPM picture scored 3.61 m after 3k steps and needed 30k to reach 2.90 m, and the held-out Procrustes pose error was still falling at step 10k (5.4 m). Readings: (a) the heat-map pose loss helps the median (3.45 vs 3.71) and should stay on (the reviewer's S1 expectation did not hold); (b) checkpoint selection by the Procrustes proxy picks a worse checkpoint than the last one (3.80 vs 3.69), so the RANSAC score should select; (c) the se2 solver is 0.1 m better than the homography for depth-placed tokens (metric scale known). Decision: the apples-to-apples test is the 30k-step budget of the picture run; submitted with the heat-map loss on, head on (8781939) and head off (8781940), evaluated best/last × srt/se2 (8781941–8781945). Verdict on the architecture waits for that.
- 2026-09-25 — **2 m cells (idea 3, `configs/vigor_cell0125.yaml`): 1.89 m median (CI 1.80–1.98) / 5.19 m mean / R@5 0.74 / R@10 0.82** on the 3000 Chicago samples (Chicago-only 30k decoder fine-tune, held-out validation, job 8777920/8777921), against 2.90 m / 5.45 m / 0.70 / 0.85 with the identical recipe at 4 m cells. Only the grid spacing changed: 0.125 m/px makes the 71 m tile fill the 112 m reference canvas (it covered 18 of 56 cells before) and halves the cell. The median moved a full metre; the tail did not (R@10 0.82 vs 0.85, mean −0.26 m), so quantisation and the black canvas were the floor of the good half, and the misses are the separate matching problem of the worst-20 sheet. Same samples: Loc² 1.33 m, FG² 1.06 m. Decision: 0.125 m/px is the VIGOR configuration from now on; submitted four-city same-area (8784448 → 8784449/8784450), cross-area (8784451 → 8784452), the Chicago 2 m checkpoint on all cities (8784457), and the Task 04 matcher at 2 m cells (8784453 → 8784454–8784456). Poznań/Durham runs keep 0.25 m/px until re-checked there (the orthophoto crop is 224 m, no black border).
- 2026-09-25 — Chicago-only 2 m-cell checkpoint on all four cities (12,000 same-area samples, job 8784457): all 2.61 m median / 5.92 m mean / R@5 0.69; Chicago 1.85 m, San Francisco 2.07 m, Seattle 2.41 m, New York 4.99 m. Against the 4 m twin (3.89 m all; Chicago 2.98, SF 3.67, Seattle 3.46, New York 5.66): the 2 m gain transfers to unseen cities (SF −1.6 m, Seattle −1.05 m) but New York moves only 0.7 m — its problem is the picture in canyons, not the grid.
- 2026-09-25 — Task 04 without the projection head (4 m cells, heat-map loss on, 30k steps, job 8781940 → 8781944/5): 4.39 m median / 7.26 m mean (best, step 28,500, se2), 4.27 m / 7.16 m (last). With the head the 10k-step run was already at 3.45 m, and the head run's held-out Procrustes error is 2 m lower at every matched step. Ablation 4b settled: the light projection head on the frozen ERP tokens (OPEN item 6) is necessary for image-plane matching with this decoder; the head stays on in every Task 04 run.
- OPEN (2026-09-24): (6) ERP-token query with a trained query-side projection head (Loc² §3.1 style: a few conv layers + self-attention on the frozen `sat493m` tokens) instead of the decoder-only fine-tune that failed in Task 6 — a new trainable module, so a design decision; (7) whether the camera-only method of record is now "IPM picture + Sat-RoMa" with the paper's contribution moving to the reference side and the filter, given that every feature-level lift lost to it.
- OPEN (not yet agreed): (1) patch-validity threshold for the matcher mask — proposed 0.5 primary with 0.25/0.75/no-mask sensitivity (package default 1.0 leaves a median of 10/196 patches); (2) single-frame vs short-mosaic query for H1 — proposed both; (3) wall-edge clutter from trees/poles — proposed LiDAR planarity filter (+ multi-frame vote in mosaics); (4) RANSAC input: published config (one peak per patch) vs distinct GMM means — proposed report both; (5) ambient-image↔ERP PnP cross-check of the camera←LiDAR extrinsic.
